<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/fa36d49c1a.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/styles/edit.css">   
    <title>Edit</title>
</head>
<body>

    <%- include('partials/header.ejs')%>
  
    <div class="divTop">
        <p>You are editing: <%=product.name%> </p>
    </div>

    <section class="allForm">
      <form action="/products/edit/<%= product.id %>?_method=PUT"
         method="POST">
          <div class="divInput">
             <label for="name">Name of the product:</label>
             <input type="text" name="name" id="name" placeholder="<%=product.name%>" required>
          </div>

          <div class="divInput">
            <label for="price">Price of the product:</label>
            <input type="number" name="price" id="price" placeholder="<%=product.price%>" required>
         </div>

         <div class="divInput">
            <label for="discount">Discount of the product:</label>
            <input type="number" name="discount" id="discount" placeholder="<%=product.discount%>" required>
         </div>

         <div class="divInput">
            <label for="msi">MSI of the product:</label>
            <input type="number" name="msi" id="msi" placeholder="<%=product.msi%>" required>
         </div>

         <div class="textArea">
            <label for="description">Description of the product:</label>
            <textarea name="description" id="description" placeholder="<%=product.description%>" required></textarea>
         </div>

         <div class="divButton">
            <button type="submit" class="saveButton">Save Product</button>
        </div>
        </form>
    </section>

    <%- include('partials/footer.ejs')%>
    <%- include('partials/modal.ejs')%>

</body>
</html>